---
"spok": minor
---

### New Features

- **Hybrid flow routing** — Run `/spok-apply hybrid <change>` to route each workflow step through its selected Claude or Codex runner while preserving the profile across resumes.
