# <parent-slug>-NN-<chunk-slug>

## Description

**User-observable behavior:** [one sentence a PM would recognize]

**Layers touched:** [db | backend | frontend | infra — list ≥2]

**Prerequisites:** [list of earlier chunk slugs, or "none"]

**End-to-end test:** [named test file/path, or describe the test to add]

**Rollback:** [one line — revert PR, flip flag, drop column, etc.]

---

[Full chunk description: what to build, references to ../`<parent-slug>`/parent-task.md, relevant `file:line` pointers from the chunkability map. Include enough context that `hl-commit-agents` can run on this ticket without re-deriving the parent context.]
